node_modules/
.next/
.env
.env.local
dist/
/.idea
/.vscode

# runtime mock DB state (regenerated on each dev run)
tmp/

# build info
*.tsbuildinfo

# local scratch (e.g. the favicon source image)
/favicon-src.*

# runtime finder photo uploads
public/uploads/*
!public/uploads/.gitkeep
